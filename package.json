{
  "name": "gastabem-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "auth-api",
    "api-gastos",
    "frontend"
  ],
  "scripts": {
    "start:auth-api": "npm start --workspace=auth-api",
    "start:api-gastos": "npm start --workspace=api-gastos",
    "start:frontend": "npm start --workspace=frontend"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
